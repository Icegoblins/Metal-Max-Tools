{
  "name": "mm2r-toolbox",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "prepare:shell": "..\\.venv\\Scripts\\python.exe ..\\准备瘦客户端dist.py",
    "prepare:shell:full": "..\\.venv\\Scripts\\python.exe ..\\准备瘦客户端dist.py --full",
    "build": "tauri build",
    "build:exe": "tauri build --no-bundle",
    "build:exe:full": "npm run prepare:shell:full && tauri build --no-bundle"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
